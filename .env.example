# ── Nex-Lyon Real Estate Analyzer ──
# Copy this file to .env and fill in your keys.
# The program works without any keys (demo mode).

# Get a free key at https://serpapi.com (for live web scraping)
SERPAPI_KEY=your_serpapi_key_here

# Get a free key at https://aistudio.google.com/apikey (for AI analysis)
GEMINI_API_KEY=your_gemini_api_key_here

# Google Sheets (see README for setup)
GOOGLE_SHEET_ID=your_google_sheet_id_here
# Local: put credentials.json in project folder
GOOGLE_CREDENTIALS_FILE=credentials.json
# Deployed (Render/Railway): paste base64-encoded credentials JSON here
# Generate with: python -c "import base64; print(base64.b64encode(open('credentials.json','rb').read()).decode())"
GOOGLE_CREDENTIALS_JSON=
